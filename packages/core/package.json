{
  "name": "@music-cards/core",
  "version": "1.0.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'No linting configured for core package'",
    "lint:fix": "echo 'No linting configured for core package'",
    "format:check": "prettier --check src tests",
    "format": "prettier --write src tests"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "jest": "^29.7.0",
    "@types/jest": "^30.0.0",
    "ts-jest": "^29.2.5",
    "prettier": "^3.6.2"
  }
}